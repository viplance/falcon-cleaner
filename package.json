{
  "name": "falcon-cleaner",
  "version": "1.1.0",
  "description": "Falcon Cleaner — native macOS app uninstaller and disk cleanup utility.",
  "private": true,
  "author": "Dzmitry Sharko",
  "license": "MIT",
  "scripts": {
    "build:app": "xcodebuild -project FalconCleaner.xcodeproj -scheme FalconCleaner -configuration Release clean build SYMROOT=$(PWD)/build",
    "release": "node ./scripts/build-release.mjs",
    "release:local": "node ./scripts/build-release.mjs --skip-notarize",
    "build:appstore": "node ./scripts/build-appstore.mjs",
    "clean": "rm -rf build .build"
  }
}
